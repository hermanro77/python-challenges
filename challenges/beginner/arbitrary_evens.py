# Define a function called arbitrary_evens that takes in an arbitrary number of arguments, 
# and returns a list containing only those arguments that are even. 
#
# Hint: modulo operator (%)

def arbitrary_evens(*nums):
	pass
